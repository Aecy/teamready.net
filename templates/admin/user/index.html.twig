{% extends 'admin/layout/card.html.twig' %}

{% block title %}
  {{ icon('user') }}
  Gestion des utilisateurs
{% endblock %}

{% block content %}
  <table class="table">
    <thead>
    <tr>
      <td></td>
      <td>Pseudo</td>
      <td>Email</td>
      <td>Premium</td>
      <td class="text-right">Actions</td>
    </tr>
    </thead>
    <tbody>
    {% for user in rows %}
      <tr>
        <td class="dashboard-id">{{ user.id }}</td>
        <td class="dashboard-id">{{ user.username }}</td>
        <td>{{ user.email }}</td>
        <td class="dashboard-id">Non premium</td>
        <td>
          <div class="dashboard-actions">
            /
          </div>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

{#  {{ paginate(rows) }}#}
{% endblock %}
